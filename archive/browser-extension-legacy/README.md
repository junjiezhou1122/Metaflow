# Legacy Browser Extension

This directory contains the superseded standalone Chrome MV3 extension.

Active development moved to:

```text
apps/chrome-acp/packages/chrome-extension
```

Keep this archive only as a migration reference and compatibility test fixture. Do not load it as the current extension or add it back to `pnpm-workspace.yaml`.
